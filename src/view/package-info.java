/**
 * das view-Package enthaelt alle Klassen mit denen der User mehr oder weniger
 * aktiv interagiert (Panels). Zusaetzlich sind im Hintergrund die
 * Controlerfunktionen enthalten (HomePanel, ActionListner)
 */
package view;